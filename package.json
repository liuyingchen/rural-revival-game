{
  "name": "rural-revival-game",
  "version": "1.0.0",
  "description": "Countryside",
  "private": true,
  "source": "index.html",
  "scripts": {
    "start": "npx live-server --port=8080 --host=0.0.0.0 --no-browser",
    "start:dev": "npx live-server --port=3000 --host=0.0.0.0 --no-browser",
    "clean": "rm -rf .cache dist .parcel-cache",
    "build": "parcel build index.html --no-source-maps --public-url ./"
  },
  "keywords": ["game", "rural-revival", "phaser"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "phaser": "^3.70.0"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "@babel/core": "^7.23.0",
    "@babel/preset-env": "^7.23.0",
    "live-server": "^1.2.2",
    "parcel": "^2.8.3",
    "@parcel/transformer-json": "^2.8.3",
    "open": "^9.1.0"
  },
  "browserslist": [
    "last 2 Chrome versions",
    "last 2 Firefox versions",
    "last 2 Safari versions"
  ]
}