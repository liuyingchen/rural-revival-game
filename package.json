{
  "name": "rural-revival-game",
  "version": "1.0.0",
  "description": "乡村振兴主题游戏",
  "main": "src/config.js",
  "scripts": {
    "clean": "rm -rf .cache dist",
    "start:dev": "live-server --port=3000 --host=localhost --no-browser --mount=/src:./src",
    "build": "parcel build index.html"
  },
  "dependencies": {
    "phaser": "^3.70.0"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "parcel-bundler": "^1.12.5"
  },
  "browserslist": [
    "last 2 Chrome versions"
  ]
}